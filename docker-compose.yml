version: '3'

services:
    web:
        build:
	        contest: .
	        dockerfile: Dockerfile.dev
	    port:
	        - "3000:3000:
	    volumes:
	        - /app/node_modules
	        - .:/app
    tests:
        build:
	        contest: .
	        dockerfile: Dockerfile.dev
		volumes:
	        - /app/node_modules
	        - .:/app
        command: ["npm", "run", "test"]